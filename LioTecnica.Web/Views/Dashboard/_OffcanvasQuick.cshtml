<div class="offcanvas offcanvas-end offcanvas-custom" tabindex="-1" id="drawerQuick">
    <div class="offcanvas-header border-bottom">
      <div>
        <div class="fw-bold">AÁıes r·pidas</div>
        <div class="text-muted small">Atalhos para operaÁ„o do RH</div>
      </div>
      <button type="button" class="btn-close" data-bs-dismiss="offcanvas"></button>
    </div>

    <div class="offcanvas-body">
      <div class="card-soft p-3 mb-3">
        <div class="fw-semibold mb-2"><i class="bi bi-plus-circle me-2"></i>Criar vaga</div>
<div class="row g-2">
          <div class="col-12">
            <label class="form-label small text-muted">TÌ≠tulo</label>
            <input class="form-control" placeholder="Ex.: Analista de Marketing Jr">
          </div>
          <div class="col-6">
            <label class="form-label small text-muted">AÅrea</label>
            <select class="form-select" id="quickArea">
              <option value="">Selecionar area</option>
            </select>
          </div>
          <div class="col-6">
            <label class="form-label small text-muted">Status</label>
            <select class="form-select" id="quickStatus" data-enum="vagaStatus"></select>
          </div>
          <div class="col-12">
            <label class="form-label small text-muted">Palavras-chave (separadas por vÌ≠rgula)</label>
            <input class="form-control" placeholder="Ex.: power bi, seo, redes sociais, crm">
          </div>
        </div>
        <div class="d-grid mt-3">
          <button class="btn btn-brand" type="button" id="btnMockCreateVaga">
            <i class="bi bi-check2 me-1"></i>Salvar
          </button>
        </div>
      </div>

      <div class="card-soft p-3 mb-3">
        <div class="fw-semibold mb-2"><i class="bi bi-inbox me-2"></i>Entrada de currÌ≠culos</div>
        <div class="d-flex flex-column gap-2">
          <button class="btn btn-ghost" type="button"><i class="bi bi-envelope me-2"></i>Conectar caixa de e-mail</button>
          <button class="btn btn-ghost" type="button"><i class="bi bi-folder2-open me-2"></i>Configurar pasta monitorada</button>
          <button class="btn btn-ghost" type="button"><i class="bi bi-play-circle me-2"></i>Rodar processamento agora</button>
        </div>
      </div>

      <div class="card-soft p-3">
        <div class="fw-semibold mb-2"><i class="bi bi-stars me-2"></i>Motor de match</div>
        <div class="small text-muted mb-2">
          Ajustes: pesos, obrigatÛrios e sinÙnimos por vaga.
        </div>
        <button class="btn btn-brand w-100" type="button">
          <i class="bi bi-sliders me-1"></i>Abrir configuraÁıes
        </button>
      </div>
    </div>
  </div>


